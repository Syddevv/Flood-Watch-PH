"use client";
import Link from "next/link";
import { AlertTriangle, Activity, CheckCircle2, Clock3, Expand, MapPin, Plus as Waves, Users } from "lucide-react";
import { FloodMap } from "@/components/flood-map";
import { getAdminOverviewData, type AdminAttentionItem, type AdminStatus, type CapacityItem } from "@/components/admin-overview-data";
import { FLOOD_LEGEND, FLOOD_POLYGONS } from "@/lib/constants";
import type { ReportActionLoadingState } from "@/lib/report-actions";

const data = getAdminOverviewData();
const statusLabel: Record<AdminStatus, string> = { critical: "Critical", urgent: "Urgent", pending: "Pending", verified: "Verified" };
const statusClass: Record<AdminStatus, string> = { critical: "bg-red-500/10 text-red-600 dark:text-red-300", urgent: "bg-orange-500/10 text-orange-600 dark:text-orange-300", pending: "bg-amber-500/10 text-amber-700 dark:text-amber-300", verified: "bg-emerald-500/10 text-emerald-700 dark:text-emerald-300" };
function StatusBadge({ status }: { status: AdminStatus }) { return <span className={`inline-flex items-center gap-1 rounded-full px-2 py-1 text-[10px] font-bold uppercase tracking-wide ${statusClass[status]}`}><span className="h-1.5 w-1.5 rounded-full bg-current" />{statusLabel[status]}</span>; }
function Card({ children, className = "" }: { children: React.ReactNode; className?: string }) { return <section className={`rounded-xl border border-[var(--color-border)] bg-[var(--color-surface)] shadow-[var(--shadow-soft)] ${className}`}>{children}</section>; }
function AttentionItem({ item }: { item: AdminAttentionItem }) { return <Link href={item.href ?? "#"} className="flex min-h-[76px] items-start justify-between gap-3 border-b border-[var(--color-border)] px-4 py-3 last:border-0 hover:bg-[var(--color-panel)]"><div className="min-w-0"><div className="truncate text-sm font-semibold">{item.title}</div><div className="mt-1 text-xs text-[var(--color-muted-foreground)]">{item.recordId} · {item.location} · {item.relativeTime}</div></div><StatusBadge status={item.status} /></Link>; }
function CapacityProgress({ item }: { item: CapacityItem }) { const percent = Math.min(100, Math.round(item.occupied / item.capacity * 100)); const color = item.status === "full" ? "bg-red-500" : item.status === "high" ? "bg-orange-500" : "bg-emerald-500"; return <div className="space-y-2"><div className="flex items-center justify-between gap-3 text-sm"><span className="truncate font-medium">{item.name}</span><span className="shrink-0 text-xs text-[var(--color-muted-foreground)]">{item.occupied}/{item.capacity}</span></div><div className="h-2 overflow-hidden rounded-full bg-[var(--color-panel)]"><div className={`h-full ${color}`} style={{ width: `${percent}%` }} /></div><div className="text-[11px] text-[var(--color-muted-foreground)]">{item.status === "full" ? "Full" : item.status === "high" ? "High occupancy" : "Available capacity"} · {percent}%</div></div>; }
function OperationalMap() { const noop = () => undefined; const actionLoading: ReportActionLoadingState = null; return <div className="h-[360px]"><FloodMap polygons={FLOOD_POLYGONS} legend={FLOOD_LEGEND} showRiskOverlays={true} onToggleRiskOverlays={noop} reportMarkers={[]} evacuationCenterMarkers={[]} showFloodReports={true} showEvacuationCenters={true} onToggleFloodReports={noop} onToggleEvacuationCenters={noop} selectedReportStatus="active" onSelectReportStatus={noop} highSeverityOnly={false} onToggleHighSeverityOnly={noop} loadingReports={false} reportLoadError={null} actionLoading={actionLoading} confirmedReportIds={{}} resolvedReportIds={{}} onSelectReport={noop} onOpenReportDetails={noop} onShareReport={() => undefined} onConfirmReport={noop} onResolveReport={noop} /></div>; }
export function AdminOverview() { return <div className="space-y-6"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold tracking-[.18em] text-[var(--color-primary)]">CALUMPIT EMERGENCY OPERATIONS</p><h2 className="mt-2 text-3xl font-bold tracking-tight">Operations overview</h2><p className="mt-2 max-w-2xl text-sm text-[var(--color-muted-foreground)]">A priority-first view of flooding, rescue operations, community reports, and evacuation readiness.</p></div><div className="flex gap-2"><Link href="/admin/map" className="inline-flex items-center gap-2 rounded-lg border border-[var(--color-border)] px-3 py-2 text-sm font-semibold hover:bg-[var(--color-panel)]"><MapPin className="h-4 w-4" />Open live map</Link><Link href="/admin/incidents/new" className="inline-flex items-center gap-2 rounded-lg bg-[var(--color-primary)] px-3 py-2 text-sm font-semibold text-white"><Waves className="h-4 w-4" />Create incident</Link></div></div>{data.dataMode === "preview" && <p className="text-xs text-[var(--color-muted-foreground)]"><span className="mr-2 inline-block h-1.5 w-1.5 rounded-full bg-[var(--color-warning)]" />Operational preview data supplements live report and center sources.</p>}<div className="flex items-start gap-3 rounded-xl border border-orange-500/30 bg-orange-500/10 p-4"><AlertTriangle className="mt-0.5 h-5 w-5 shrink-0 text-orange-500" /><div><div className="font-semibold">{data.alert.title}</div><p className="mt-1 text-sm text-[var(--color-muted-foreground)]">{data.alert.description}</p><p className="mt-2 text-xs font-medium text-orange-600 dark:text-orange-300">{data.alert.updated}</p></div></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{data.stats.map((stat) => <Card key={stat.label} className="p-4"><div className="flex items-center justify-between"><p className="text-xs font-semibold uppercase tracking-wide text-[var(--color-muted-foreground)]">{stat.label}</p><Activity className="h-4 w-4 text-[var(--color-primary)]" /></div><p className="mt-3 text-3xl font-bold tabular-nums">{stat.value}</p><p className="mt-1 text-xs text-[var(--color-muted-foreground)]">{stat.detail}</p></Card>)}</div><div className="grid gap-6 xl:grid-cols-[minmax(0,1.7fr)_minmax(340px,1fr)]"><Card className="overflow-hidden"><div className="flex items-center justify-between border-b border-[var(--color-border)] px-5 py-4"><div><h3 className="font-semibold">Live operational map</h3><p className="text-xs text-[var(--color-muted-foreground)]">Flood impact, rescue requests, incidents, and shelters.</p></div><Link href="/admin/map" aria-label="Expand live operational map" className="rounded-lg p-2 text-[var(--color-muted-foreground)] hover:bg-[var(--color-panel)]"><Expand className="h-4 w-4" /></Link></div><OperationalMap /></Card><Card><div className="border-b border-[var(--color-border)] px-5 py-4"><h3 className="font-semibold">Needs attention now</h3><p className="text-xs text-[var(--color-muted-foreground)]">Prioritized by life-safety risk and waiting time.</p></div><div>{data.attentionItems.map((item) => <AttentionItem key={item.id} item={item} />)}</div></Card></div><div className="grid gap-6 lg:grid-cols-2"><Card className="p-5"><div className="flex items-center justify-between"><div><h3 className="font-semibold">Evacuation readiness</h3><p className="text-xs text-[var(--color-muted-foreground)]">918 of 1,140 available spaces currently occupied.</p></div><Users className="h-5 w-5 text-[var(--color-primary)]" /></div><div className="mt-5 space-y-5">{data.evacuationReadiness.map((item) => <CapacityProgress key={item.id} item={item} />)}</div></Card><Card className="p-5"><div className="flex items-center justify-between"><div><h3 className="font-semibold">Recent operational activity</h3><p className="text-xs text-[var(--color-muted-foreground)]">Latest actions across the EOC.</p></div><Clock3 className="h-5 w-5 text-[var(--color-primary)]" /></div><div className="mt-5 space-y-5">{data.activity.map((entry) => <div key={entry.id} className="flex gap-3"><CheckCircle2 className="mt-0.5 h-4 w-4 shrink-0 text-emerald-500" /><div className="min-w-0"><p className="text-sm font-medium">{entry.action} <span className="text-[var(--color-primary)]">{entry.record}</span></p><p className="mt-1 text-xs text-[var(--color-muted-foreground)]">{entry.description}</p><p className="mt-1 text-[11px] text-[var(--color-muted-foreground)]">{entry.timestamp}</p></div></div>)}</div></Card></div></div>; }
