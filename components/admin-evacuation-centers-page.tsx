"use client";
/* eslint-disable react-hooks/set-state-in-effect */

import Link from "next/link";
import { ChevronRight, MapPin, Plus, Search } from "lucide-react";
import { useCallback, useEffect, useState } from "react";

export type AdminCenter = { id: string; name: string; address: string; barangay: string | null; city: string; province: string; latitude: number; longitude: number; facilities: string[]; status: string; verificationStatus: string; estimatedCapacity: number | null; lastVerifiedAt: string | null; updatedAt: string };

function statusTone(status: string) { const value = status.toLowerCase(); if (value === "available") return "border-emerald-500/40 bg-emerald-500/10 text-emerald-500"; if (value.includes("unavailable")) return "border-red-500/40 bg-red-500/10 text-red-500"; return "border-amber-500/40 bg-amber-500/10 text-amber-500"; }
function label(value: string) { return value.replaceAll("_", " ").replace(/\b\w/g, (letter) => letter.toUpperCase()); }

export function AdminEvacuationCentersPage() {
  const [centers, setCenters] = useState<AdminCenter[]>([]); const [search, setSearch] = useState(""); const [loading, setLoading] = useState(true); const [error, setError] = useState<string | null>(null);
  const load = useCallback(async () => { setLoading(true); setError(null); try { const response = await fetch(`/api/admin/evacuation-centers?search=${encodeURIComponent(search)}`, { cache: "no-store" }); const body = await response.json(); if (!response.ok) throw new Error(body.error ?? "Unable to load evacuation centers."); setCenters(body.data.centers); } catch (cause) { setError(cause instanceof Error ? cause.message : "Unable to load evacuation centers."); } finally { setLoading(false); } }, [search]);
  useEffect(() => { void load(); }, [load]);
  return <div className="space-y-6"><header className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold tracking-[.18em] text-[var(--color-primary)]">CALUMPIT EMERGENCY OPERATIONS</p><h2 className="mt-2 text-3xl font-bold tracking-tight">Evacuation centers</h2><p className="mt-2 text-sm text-[var(--color-muted-foreground)]">Manage verified capacity, facilities, accessibility, and center operating status.</p></div><Link href="/admin/data/new" className="inline-flex h-10 items-center gap-2 rounded-lg bg-[var(--color-primary)] px-4 text-sm font-semibold text-white"><Plus className="h-4 w-4" />Add center</Link></header><label className="flex max-w-md items-center gap-2 rounded-lg border border-[var(--color-border)] bg-[var(--color-surface)] px-3"><Search className="h-4 w-4 text-[var(--color-muted-foreground)]" /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search evacuation centers" className="min-w-0 flex-1 bg-transparent py-2.5 text-sm outline-none" /></label>{error && <p className="rounded-lg border border-red-500/30 bg-red-500/10 p-4 text-sm text-red-400">{error}</p>}{loading ? <p className="text-sm text-[var(--color-muted-foreground)]">Loading evacuation centers...</p> : centers.length ? <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{centers.map((center) => <article key={center.id} className="flex min-h-[300px] flex-col rounded-xl border border-[var(--color-border)] bg-[var(--color-surface)] p-5"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><h3 className="font-semibold">{center.name}</h3><p className="mt-1 flex items-start gap-1 text-xs text-[var(--color-muted-foreground)]"><MapPin className="mt-0.5 h-3 w-3 shrink-0" />{center.barangay ? `Brgy. ${center.barangay}` : center.address}</p></div><span className={`shrink-0 rounded-lg border px-2 py-2 text-xs font-medium ${statusTone(center.status)}`}>{label(center.status)}</span></div><div className="mt-7"><div className="flex justify-between text-sm"><span>Estimated capacity</span><strong>{center.estimatedCapacity?.toLocaleString() ?? "Not recorded"}</strong></div><div className="mt-3 h-2 rounded-full bg-[var(--color-panel)]"><div className="h-full w-0 rounded-full bg-[var(--color-primary)]" /></div></div><p className="mt-5 text-sm font-medium">{center.city}, {center.province}</p><div className="mt-4 flex flex-wrap gap-1.5">{center.facilities.length ? center.facilities.slice(0, 4).map((facility) => <span key={facility} className="rounded-md bg-[var(--color-panel)] px-2 py-1 text-[11px] font-medium">{label(facility)}</span>) : <span className="text-xs text-[var(--color-muted-foreground)]">No facilities recorded</span>}</div><p className="mt-4 text-xs text-[var(--color-muted-foreground)]">Verification: {label(center.verificationStatus)}</p><Link href={`/admin/data/${center.id}`} className="mt-auto flex h-10 items-center justify-center gap-2 rounded-lg border border-[var(--color-border)] text-sm font-semibold hover:bg-[var(--color-panel)]">Manage center <ChevronRight className="h-4 w-4" /></Link></article>)}</div> : <p className="rounded-xl border border-dashed border-[var(--color-border)] p-8 text-center text-sm text-[var(--color-muted-foreground)]">No evacuation centers match this search.</p>}</div>;
}
