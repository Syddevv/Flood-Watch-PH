"use client";
import { useState, type FormEvent } from "react";
import { Plus, X } from "lucide-react";
export function AdminCreateReportModal({ onCreated }: { onCreated: () => void }) { const [open,setOpen]=useState(false); const [busy,setBusy]=useState(false); const [error,setError]=useState(""); const [form,setForm]=useState({title:"",description:"",category:"Flooding",severity:"Moderate",locationName:"",latitude:"14.916",longitude:"120.765",reporterName:""}); async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");try{const r=await fetch("/api/admin/reports",{method:"POST",headers:{"Content-Type":"application/json",Origin:window.location.origin},body:JSON.stringify({...form,latitude:Number(form.latitude),longitude:Number(form.longitude)})});const b=await r.json();if(!r.ok)throw new Error(b.error);setOpen(false);onCreated();}catch(cause){setError(cause instanceof Error?cause.message:"Unable to create report.")}finally{setBusy(false)}} return <><button type="button" onClick={()=>setOpen(true)} className="inline-flex items-center gap-2 rounded-lg bg-[var(--color-primary)] px-3 py-2 text-sm font-semibold text-white"><Plus className="h-4 w-4"/>New report</button>{open?<div role="dialog" aria-modal="true" className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 p-4"><form onSubmit={submit} className="w-full max-w-xl rounded-lg bg-[var(--color-surface)] p-5"><div className="flex justify-between"><h2 className="text-lg font-semibold">Create flood report</h2><button type="button" onClick={()=>setOpen(false)} aria-label="Close"><X className="h-5 w-5"/></button></div>{error&&<p className="mt-3 text-sm text-red-400">{error}</p>}<div className="mt-4 grid gap-3 sm:grid-cols-2">{([['title','Title'],['locationName','Location'],['reporterName','Reporter name'],['latitude','Latitude'],['longitude','Longitude']] as const).map(([k,l])=><label key={k} className="text-sm">{l}<input required={k!=='reporterName'} value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})} className="mt-1 w-full rounded border px-3 py-2"/></label>)}<label className="text-sm">Category<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})} className="mt-1 w-full rounded border px-3 py-2"><option>Flooding</option><option>Road blocked</option><option>Infrastructure damage</option><option>Other</option></select></label><label className="text-sm">Severity<select value={form.severity} onChange={e=>setForm({...form,severity:e.target.value})} className="mt-1 w-full rounded border px-3 py-2"><option>Low</option><option>Moderate</option><option>High</option><option>Critical</option></select></label></div><label className="mt-3 block text-sm">Description<textarea required rows={4} value={form.description} onChange={e=>setForm({...form,description:e.target.value})} className="mt-1 w-full rounded border p-3"/></label><div className="mt-4 flex justify-end gap-2"><button type="button" onClick={()=>setOpen(false)} className="rounded border px-3 py-2 text-sm">Cancel</button><button disabled={busy} className="rounded bg-[var(--color-primary)] px-3 py-2 text-sm font-semibold text-white">{busy?'Creating...':'Create report'}</button></div></form></div>:null}</> }
